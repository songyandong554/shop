import { reqSubmitOrder } from "@/api";
const actions = {};
//准备mutations——用于操作数据(state)
const mutations = {};
//准备state——用于存储数据
const state = {};
//准备getters——用于将state中的数据进行加工
const getters = {};

//创建并暴露store
export default {
  actions,
  mutations,
  state,
  getters,
};
